import styled from 'styled-components';
import { Circle} from '@mui/icons-material';

export const StyledLogo = styled(Circle)`
  font-size: 2rem !important;
  margin-right: 0.25rem;
`;

export const LogoWrapper = styled.div`
  display: flex;
  align-items: center;
  padding-left: 1rem;
  font-weight: 400;
  font-size: 1.25rem;
`;